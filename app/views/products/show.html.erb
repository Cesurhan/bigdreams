<hr>
<div class="row">
  <div class="col-sm-6 col-md-4">
    <%= image_tag @product.photo, style: 'height:20em'%>
    <div class="caption">
      <h3><%= @product.name %></h3>
      <p><%= "€#{@product.price}"%></p>
      <p><%= @product.description%></p>
      <p>
        <%= link_to "Add to cart", {:controller => :shopping_cart, :action => 'create', :product_id => @product.id }, :method => :create,class: "btn btn-primary" %>
        <% if user_signed_in? && current_user.admin?%>
        <%= link_to "Edit", edit_product_path, class:"btn btn-primary"%>
        <%= link_to "Delete", product_path(@product.id), method: :delete, data: {confirm: "Are you sure?"}, class:"btn btn-danger"%>
      </div>
      <%end%><%= link_to "Back", products_path, class:"btn btn-default"%>
    </p>
  </div>
</div>
  <%= render "shared/shopping_cart"%>
